import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Activities | Our Budget',
    description: 'Check in on all current activities.',
}

export default function Activities() {
    return (
        <main>

        </main>
    )
}