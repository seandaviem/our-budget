import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Manage | Our Budget',
    description: 'Manage various settings for your current budget portal.',
}

export default function Manage() {
    return (
        <main>

        </main>
    )
}